# The bridge runs with zero configuration — every value below has a sensible
# default baked in. Copy this file to ".env" (next to the executable when
# packaged) only if you need to override something.

# Bloomberg Terminal connection (defaults match a local Bloomberg Terminal).
BLOOMBERG_HOST=localhost
BLOOMBERG_PORT=8194

# Local HTTP/WebSocket server the explorer talks to.
APP_HOST=127.0.0.1
APP_PORT=8000

# EXTRA browser origins allowed to call the bridge (comma separated).
# The hosted Universe Studio explorer and localhost dev ports are ALWAYS
# allowed (baked into the build); this only ADDS origins. Leave unset unless
# you need another origin, e.g.:
# CORS_ORIGINS=https://staging.thesimplereport.com
